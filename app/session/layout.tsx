export const metadata = {
  title: 'New Session - Sessional AI',
  description: 'Record a new session with AI assistance',
}

export default function SessionLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
} 