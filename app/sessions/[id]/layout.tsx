export const metadata = {
  title: 'Session Details - Sessional AI',
  description: 'View session details and recordings',
}

export default function SessionLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
} 